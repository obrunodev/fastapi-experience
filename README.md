# fastapi-template
Um template de FastAPI com SQLAlchemy, Alembic e autenticação

## Como usar

### Instalar as dependências

```bash
uv sync
```

### Executar a API

```bash
uv run uvicorn apps.main:app --reload
```

### Comando via Makefile

```bash
make install
make run
```
